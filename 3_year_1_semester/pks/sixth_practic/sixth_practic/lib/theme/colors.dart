// lib/theme/colors.dart
import 'package:flutter/material.dart';

// Вынос цветов в отдельный класс для удобства
class AppColors {
  static const Color primary = Color(
    0xFF1E88E5,
  ); // Пример цвета, синий [cite: 210]
  static const Color accent = Color(
    0xFFFFC107,
  ); // Пример акцентного цвета, желтый [cite: 211]
  static const Color background = Color(0xFFF5F5F5); // Светлый фон [cite: 212]
  static const Color textDark = Color(0xFF212121);
  static const Color textLight = Color(0xFFFFFFFF);
}
